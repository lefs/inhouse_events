class CreateInhouseEvents < ActiveRecord::Migration<% if Rails::VERSION::MAJOR >= 5 %>[<%= Rails::VERSION::MAJOR %>.<%= Rails::VERSION::MINOR %>]<% end %>
  def change
    create_table :inhouse_events do |t|
      t.string :client_event_id
      t.string :server_event_id
      t.string :event_type
      t.datetime :client_timestamp
      t.datetime :server_timestamp
      t.string :client_timezone
      t.string :page_title
      t.string :referrer_url
      t.string :user_agent
      t.string :ip_address
      t.string :page_url
      t.string :user
      t.string :session_id
      t.string :visit_id
    end
  end
end
